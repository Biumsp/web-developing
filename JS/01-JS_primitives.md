# JavaScript Primitives & The Chrome Console

You can run code in the Chrome's console.   
It performs a Read-Evaluate-Print Loop (REPL).  

typeof returns the type of an object. NaN is a number.

To create a variable is
```
let name = value;
```

